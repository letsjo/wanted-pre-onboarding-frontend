import { Styled } from './style';

const Header = () => {
  return (
    <Styled.Container>
      <Styled.Title>To Do List</Styled.Title>
      <Styled.Button>로그아웃</Styled.Button>
    </Styled.Container>
  );
};

export default Header;
