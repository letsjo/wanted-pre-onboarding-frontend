import styled from 'styled-components';
import { flexCenter } from 'styles/modules';

export const Styled = {
  Container: styled.form`
    ${flexCenter('row')}
    width: 80%;
    margin-top: 5px;
  `,
};
